# Django Configuration
DJANGO_ENV=prod
SECRET_KEY=CHANGE_THIS_TO_RANDOM_SECRET_KEY
DEBUG=False
ALLOWED_HOSTS=app-domain.com

# Database Configuration
POSTGRES_DB=roommate
POSTGRES_USER=roommate_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD

# Email Configuration (optional)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@app-domain.com

# Let's Encrypt Configuration
CERTBOT_EMAIL=your-email@example.com
CERTBOT_DOMAIN=app-domain.com

# Cloudflare Configuration
# Get these from Cloudflare dashboard: https://dash.cloudflare.com/profile/api-tokens
# Create a token with Zone:DNS:Edit permissions
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
